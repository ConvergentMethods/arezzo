# N1: Create a named range

Named range 'executive_summary' spans 14-99.

**Index arithmetic:** None — named ranges are metadata.

**Key detail:** Range stored in documentTab.namedRanges, not in body content.