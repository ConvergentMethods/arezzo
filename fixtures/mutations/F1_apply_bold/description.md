# F1: Apply bold to a text range

Bolds '15%' at indices 143-146.

**Index arithmetic:** None — formatting doesn't change indices.

**Key detail:** `fields` mask is critical. Omitting it clears all other styles.